# AI Study Assistant — Backend (المرحلة 1-8: كل الميزات الأساسية + جاهزية إنتاج مُتحقَّق منها)

Backend فعلي وقابل للتشغيل مبني بـ **FastAPI**، يطبّق دورة MVP كاملة (§33):
تسجيل حساب → دخول → رفع ملف → استخراج نصه (نصيًا أو عبر OCR) وتقسيمه →
تلخيص/محادثة عبره → توليد اختبار وبطاقات تعليمية بالذكاء الاصطناعي وحفظ
نتائجها → تجميع إحصائيات تقدّم حقيقية (بما فيها وقت دراسة فعلي).

## 🎉 تحديثان كبيران في هذه المرحلة

**1) تم التحقق فعليًا من استدعاء Gemini حقيقي** (وليس فقط مسارات الخطأ):
بعد جلسات من بناء مسارات 503/502 فقط، **نجح استدعاء فعلي حقيقي** من مستخدم
حقيقي بمفتاح حقيقي. اكتُشف أثناء ذلك أن `gemini-2.0-flash` توقّف عن الدعم
رسميًا، وتم تحديثه لـ `gemini-3.6-flash` (البديل الذي أوصت به Google
نفسها في رسالة الخطأ). راجع `diagnose_gemini.py`.

**2) جاهزية إنتاج حقيقية، مُختبَرة فعليًا وليست افتراضًا نظريًا**: تم فعليًا
تثبيت PostgreSQL وRedis وTesseract OCR في بيئة الاختبار، وتشغيل كامل حزمة
الاختبارات ضدها — وليس فقط "من المفترض أن يعمل".

## ✅ تم تنفيذه واختباره فعليًا (73 اختبارًا ناجحًا + 6 اختبارات ذاكرة محادثة + 7 اختبارات Google Sign-In)

| الميزة | الحالة |
|---|---|
| تسجيل حساب / دخول / `/me` | ✅ يعمل، JWT حقيقي + bcrypt |
| رفع ملف (PDF/JPG/PNG) مع تحقق من الحجم/النوع | ✅ يعمل |
| **استخراج نص PDF + OCR فعلي للملفات الممسوحة ضوئيًا** (§7) | ✅ **مُتحقَّق منه بملف PDF حقيقي بلا طبقة نص** — استخرج Tesseract النص بدقة 100% |
| **محادثة عامة بلا رفع ملف** + محادثة RAG (`/api/ai/chat`) + تلخيص (`/summary`) | ✅ **مُتحقَّق منه باستدعاء Gemini فعلي ناجح** |
| **سجل محادثة محفوظ فعليًا** (`GET /api/ai/chat/history`) | ✅ **مُتحقَّق منه فعليًا** — منفصل بين محادثة عامة وكل ملف |
| **توليد أسئلة MCQ بالذكاء الاصطناعي** (`POST /api/documents/{id}/questions`) | ✅ مبني، بنفس النموذج المُتحقَّق منه أعلاه — راجع القيد أدناه |
| **خوض الاختبار + تصحيح وحفظ النتيجة** | ✅ **مُتحقَّق منه فعليًا** — لا يُسرّب الإجابة، يرفض بيانات غير مطابقة |
| **توليد بطاقات تعليمية بالذكاء الاصطناعي** | ✅ مبني، بنفس النموذج المُتحقَّق منه — راجع القيد أدناه |
| **Spaced Repetition حقيقي** (§15: 1→3→7→14 يومًا + `?due_only=true`) | ✅ **مُتحقَّق منه رقميًا بالساعة** |
| **Refresh Tokens** (`POST /api/auth/refresh`) | ✅ **مُتحقَّق منه فعليًا** — access (يوم) + refresh (30 يومًا) |
| **Rate Limiting عبر Redis** (20 طلب/ساعة) مع تراجع تلقائي للذاكرة | ✅ **مُتحقَّق منه فعليًا** — تحقّقت من المفاتيح مباشرة عبر `redis-cli` |
| **StudySessions حقيقية** مع سقف أمان للجلسات المفتوحة | ✅ **مُتحقَّق منه فعليًا** |
| **إحصائيات التقدّم من نشاط حقيقي شامل** (`GET /api/progress`) | ✅ **مُتحقَّق منه فعليًا بدقة رقمية** |
| **PostgreSQL** (بدل SQLite) | ✅ **مُتحقَّق منه فعليًا** — كل الاختبارات الـ73 تعمل ضده مباشرة |
| **Alembic Migrations** | ✅ **مُتحقَّق منه فعليًا** — upgrade/downgrade يعملان على PostgreSQL وSQLite معًا (بما فيها إصلاح `batch_alter_table` لـ SQLite) |
| **Google Sign-In** (`POST /api/auth/google`) | ✅ **منطق الخادم مُتحقَّق منه بالكامل** (7 سيناريوهات) — **مسار OAuth الفعلي من الجهاز غير مُختبَر** (يحتاج إعدادك في Google Cloud Console) |
| **ذاكرة محادثة حقيقية داخل نفس المحادثة** | ✅ **مُتحقَّق منه فعليًا** (بعميل Gemini مُزيَّف) — كل سؤال يُرسَل مع آخر 10 رسائل كسياق فعلي |
| عرض/حذف الملفات (يحذف كل بياناتها التابعة) | ✅ يعمل، معزول لكل مستخدم |

## 💰 مجاني بالكامل — Google Gemini بدل OpenAI

بناءً على طلب صريح، تم استبدال OpenAI بالكامل بـ **Google Gemini** (الطبقة
المجانية، بلا بطاقة بنكية):
- المحادثة/التلخيص/التوليد: `gemini-3.6-flash`
- الـ Embeddings (للبحث الدلالي RAG): `text-embedding-004`

**الملف الوحيد** الذي يعرف اسم المزوّد الفعلي هو `app/services/ai_service.py`
— بقية الكود يستدعي فقط `embed_text()`/`chat_completion()` دون أي معرفة
بالمزوّد (§26). احصل على مفتاح مجاني من https://aistudio.google.com/apikey.

### ⚠️ قيد لا يزال قائمًا: توليد الأسئلة والبطاقات

`chat` و`summary` **تم التحقق منهما فعليًا** باستدعاء Gemini حقيقي ناجح.
`questions` (توليد أسئلة Quiz) و`flashcards` يستخدمان بالضبط نفس الدالة
(`chat_completion`)، لكن **لم يُشاهَد ناتجهما الفعلي بنفس التفصيل بعد** —
تحديدًا: هل يُعيد `gemini-3.6-flash` فعليًا JSON صالحًا بالشكل المطلوب
لتحليله (`quiz_generation.py`/`flashcard_generation.py`)، أم يحتاج تعديل
الـ Prompt؟ **جرّبهما فعليًا من التطبيق** قبل الاعتماد عليهما بثقة كاملة.

## 🪟 إصلاح Windows: خطأ ترميز عند بدء التشغيل (تحديث جديد)

إذا واجهت `UnicodeDecodeError: 'charmap' codec can't decode byte...` عند
تشغيل `uvicorn app.main:app --reload` على Windows: هذا كان خطأ حقيقيًا
اكتُشف فعليًا — `alembic.ini` كان يحتوي تعليقًا بالعربية، وWindows يفتح
ملفات `.ini` بترميز النظام الافتراضي (غالبًا `cp1252`) بدل UTF-8، فيفشل
عند أي حرف غير ASCII. **تم إصلاحه**: `alembic.ini` أصبح ASCII خالصًا
بالكامل (تحقّقنا من ذلك حرفيًا بمحاكاة قراءته بـ`cp1252`)، و`.env`
أصبح يُقرأ بـ`utf-8` صراحةً (`env_file_encoding` في `config.py`) كإجراء
احترازي إضافي. أي ملف `.ini`/إعداد آخر يُضاف مستقبلًا لهذا المشروع يجب أن
يبقى ASCII خالصًا لنفس السبب — التعليقات التوضيحية بالعربية تذهب لملفات
`.py` أو `.md` فقط (تُقرأ بـUTF-8 دائمًا بغض النظر عن نظام التشغيل).

## 👤 تحديث الملف الشخصي (تحديث جديد)

`PATCH /api/auth/me` يسمح بتحديث الاسم/التخصص/المستوى الدراسي جزئيًا —
أي حقل لا يُرسَل يبقى بلا تغيير. **مُختبَر بـ4 سيناريوهات**: تحديث جزئي
صحيح، عدم تأثّر الحقول غير المُرسَلة، ورفض الطلبات غير المُصادَق عليها.

## 🔐 Google Sign-In حقيقي (تحديث جديد)

`POST /api/auth/google` يقبل ID Token من Flutter، يتحقق منه **فعليًا مع
خوادم Google** (توقيع رقمي حقيقي، وليس فك تشفير محلي)، ثم:
- ينشئ حسابًا جديدًا تلقائيًا إن كانت أول مرة
- يسجّل الدخول لنفس الحساب في المرات التالية (بدون تكرار)
- **يربط تلقائيًا** بحساب بريد/كلمة مرور موجود مسبقًا بنفس البريد الإلكتروني
  (بدل إنشاء حساب مكرَّر)، مع بقاء تسجيل الدخول بكلمة المرور القديمة يعمل

**تم اختباره فعليًا بـ7 سيناريوهات** بتزييف تحقق Google (`test_google_auth.py`)
— بما فيها الحالة الحسّاسة: مستخدم Google الخالص (بلا كلمة مرور إطلاقًا)
يحاول تسجيل الدخول بكلمة مرور فيُرفَض بـ401 نظيف بدل انهيار داخلي (كان
هذا bug حقيقي محتمل بسبب جعل `hashed_password` قابلة لـ NULL — أُصلح
بالتحقق الصريح قبل استدعاء `verify_password`).

**تم اكتشاف وإصلاح خطأ Migration حقيقي أثناء هذا العمل**: SQLite لا يدعم
`ALTER COLUMN ... DROP NOT NULL` (خلافًا لـ PostgreSQL الذي يدعمه بلا
مشاكل) — الحل: `batch_alter_table` بدل `alter_column` المباشر، يعمل على
قاعدتي البيانات معًا. تم التحقق من upgrade/downgrade فعليًا على كليهما.

**ما يحتاج إعدادك أنت (لا يمكن تجاوزه):**
1. أنشئ مشروع Google Cloud (أو استخدم نفس مشروع Gemini)
2. من https://console.cloud.google.com/apis/credentials أنشئ **Web OAuth
   Client ID**، وضع قيمته في `.env` كـ `GOOGLE_CLIENT_ID=...`
3. لـ Android: أنشئ أيضًا Android OAuth Client ID (يحتاج SHA-1 fingerprint
   + application ID)، وفي Flutter ضع نفس الـ Web Client ID في
   `lib/core/network/google_auth_config.dart` كـ `serverClientId`
4. **هذا الجزء تحديدًا لم يُختبَر فعليًا من طرفي** — لا أملك بيانات اعتماد
   Google Cloud حقيقية. اختبرت فقط منطق الخادم بعد وصول رمز صالح (بتزييف
   التحقق)، وليس مسار OAuth الكامل من الجهاز إلى Google وعودةً.

```bash
python test_google_auth.py
```

## ⚡ تحسينات الأداء (تحديث جديد)

- **إصلاح مشكلة N+1 استعلام حقيقية في `/api/progress`**: كان الوصول لـ
  `attempt.quiz.document.title` لكل محاولة اختبار يُنفّذ استعلامين إضافيين
  منفصلَين (Lazy Loading الافتراضي في SQLAlchemy) — مع مستخدم لديه 20
  محاولة، كان هذا يعني ~41 استعلامًا بدل استعلام واحد مُجمَّع. أُصلح عبر
  `joinedload`، **وتحقّقت رقميًا** (عبر عدّاد استعلامات SQLAlchemy فعلي،
  وليس افتراضًا): **4 استعلامات فقط** بدل ~41 لنفس السيناريو.
  ```bash
  python test_query_performance.py
  ```

## 🧠 ذاكرة محادثة حقيقية (تحديث جديد)

كل سؤال جديد يُرسَل الآن مع **آخر 10 رسائل من نفس المحادثة** كسياق فعلي
لـ Gemini (وليس بمعزل تام عن الأسئلة السابقة كما كان سابقًا). يعمل هذا
تلقائيًا لكل من المحادثة العامة ومحادثة كل ملف على حدة (السياقات منفصلة
تمامًا بينهما).

**تم اختباره فعليًا بعميل Gemini مُزيَّف** (`test_conversation_memory.py`)
— بدون حاجة لمفتاح حقيقي، لأن هذا اختبار لمنطق تجميع السياق نفسه (ترتيب
الأدوار، تحويل `assistant`→`model` كما يتوقّع Gemini، تسلسل الرسائل)
وليس لجودة استجابة النموذج. النتيجة: 6/6 اختبارات ناجحة تؤكد أن السؤال
الثاني في أي محادثة يصل فعليًا مع الأول وإجابته كسياق، وليس منفصلًا عنه.

```bash
python test_conversation_memory.py
```

## 🏭 جاهزية الإنتاج — تفاصيل ما تم اختباره فعليًا

### ⚠️ عند النشر على Render/Railway: ثبّت إصدار Python صراحةً

اكتُشف فعليًا: منصات الاستضافة قد تستخدم أحدث إصدار Python متاح تلقائيًا
(Render غيّر افتراضيه لـ 3.14.3 في فبراير 2026)، بينما `psycopg2-binary`
(وحزم أخرى تعتمد على امتدادات C) قد لا تملك بعد نسخة ثنائية متوافقة معه،
فيفشل التشغيل بخطأ `ImportError: undefined symbol: _PyInterpreterState_Get`.

**لـ Render تحديدًا**: جرّبنا أولًا ملف `runtime.txt` (طريقة Heroku
التقليدية) — **لم يعمل**؛ توثيق Render الرسمي يؤكد أن بيئة Python
الأصلية لديهم لا تقرأ هذا الملف إطلاقًا. الطريقة الصحيحة الوحيدة: متغير
بيئة `PYTHON_VERSION` (مثل `3.12.7`) يُضاف من لوحة تحكم الخدمة مباشرة
(Environment) — نفس الإصدار المُختبَر محليًا طوال هذا المشروع. لا يحتاج
أي ملف أو رفع جديد لـ GitHub، فقط حفظ المتغير وإعادة النشر.


### PostgreSQL
```bash
# في .env:
DATABASE_URL=postgresql://user:password@localhost:5432/ai_study_db
```
يحتاج `psycopg2-binary` (موجود أصلًا في `requirements.txt`). **تم تشغيل
كامل حزمة الاختبارات (73/73) ضد PostgreSQL 16 فعليًا** في بيئة الاختبار،
وليس افتراضًا لأن "SQLAlchemy يدعم كلاهما نظريًا".

### Alembic Migrations — تُطبَّق تلقائيًا الآن عند بدء التشغيل

**تحديث مهم**: `app/main.py` يُطبّق `alembic upgrade head` تلقائيًا في كل
مرة يبدأ فيها التطبيق — لم يعد `create_all` مُستخدَمًا إطلاقًا. هذا يحل
مشكلة شائعة كانت تحدث سابقًا: تعديل نموذج (مثل إضافة `google_id`) ثم
الحصول على `no such column` عند التشغيل، لأن `create_all` **لا يُحدّث
جداول موجودة مسبقًا** — ينشئ الجداول المفقودة فقط.

**الأهم**: هذا يعمل تلقائيًا حتى مع قاعدة بيانات **قديمة** كانت أُنشئت
سابقًا عبر `create_all` (قبل اعتماد Alembic في هذا المشروع) — الكود
يكتشف هذه الحالة تحديدًا (وجود جدول `users` بلا جدول `alembic_version`)
ويُعلِّم (`stamp`) القاعدة عند أول Migration تلقائيًا بدل محاولة إعادة
إنشاء جداول موجودة أصلًا (وهذا كان سيفشل بخطأ "table already exists").
**تم اختبار هذا فعليًا بمحاكاة قاعدة بيانات قديمة حقيقية فيها بيانات،
والتأكد أن لا شيء يُفقَد وأن العمود الجديد يُضاف بشكل صحيح.**

عند تعديل أي نموذج مستقبلًا:
```bash
alembic revision --autogenerate -m "وصف التغيير"
```
ثم شغّل التطبيق عاديًا (`uvicorn app.main:app --reload`) — الترحيل
يُطبَّق تلقائيًا، بلا أي خطوة يدوية إضافية وبلا حذف أي قاعدة بيانات أبدًا.

أوامر يدوية إضافية عند الحاجة:
```bash
alembic upgrade head      # تطبيق يدوي (نادرًا ما تحتاجه، يحدث تلقائيًا)
alembic downgrade base    # للتراجع الكامل (اختبرناه فعليًا أيضًا)
```
`migrations/env.py` يقرأ `DATABASE_URL` من نفس `.env` الذي يستخدمه
التطبيق تلقائيًا — لا إعداد مزدوج. **تم اختبار upgrade وdowngrade فعليًا
على PostgreSQL وSQLite معًا.**

### Redis + حد الطلبات (Rate Limiting) — تحديث: لا حدّ عملي افتراضيًا

```bash
# في .env (اختياري):
REDIS_URL=redis://localhost:6379/0
AI_RATE_LIMIT_PER_HOUR=10000   # الافتراضي — فعليًا بلا حدّ عملي على الاستخدام الطبيعي
```

**بناءً على طلب صريح**: الحد لم يعد ثابتًا مبرمَجًا (كان 20 طلب/ساعة) —
أصبح قابلًا للتعديل عبر `AI_RATE_LIMIT_PER_HOUR`، وقيمته الافتراضية
عالية جدًا (10,000) بحيث لا يشعر بها أي استخدام طبيعي عمليًا.

**⚠️ ملاحظة مهمة يجب معرفتها**: هذا الإعداد يتحكم فقط بحدّنا الداخلي —
**Google نفسها تفرض حصة منفصلة تمامًا على مستوى مفتاح Gemini** (يومية
وبالدقيقة معًا)، خارج سيطرة هذا الكود كليًا. رفع هذا الرقم أو تعطيله لا
"يُلغي" حدود Google الفعلية — فقط يمنع تطبيقنا من حجب المستخدم *قبل*
الوصول لحدود Google. إذا واجهت أخطاء 429 رغم هذا الإعداد، فمصدرها حصة
Google نفسها (راجع `console.cloud.google.com` لمراقبة الاستهلاك الفعلي)،
وليس هذا الكود.

بدون Redis، يتراجع النظام تلقائيًا لحدّ في ذاكرة العملية (كافٍ لسيرفر
تطوير واحد، غير كافٍ لعدة نسخ في الإنتاج). **تم تشغيل الاختبارات ضد Redis
حقيقي والتحقق مباشرة عبر `redis-cli GET`/`TTL`** أن العدّاد ومدة الصلاحية
صحيحان فعليًا، وأيضًا اختبار حالة Redis معطوب/غير قابل للوصول (يتراجع
بأمان دون كسر السيرفر).

### قراءة الملفات الممسوحة ضوئيًا عبر Gemini Vision (§7) — تحديث كبير

**خطأ حقيقي إضافي اكتُشف واصلُح في نفس هذا التحديث**: الصور المرفوعة
**مباشرة** (JPG/PNG عبر "تصوير بالكاميرا" أو "اختيار صورة" في Flutter —
وليست صورة مضمَّنة داخل ملف PDF) كانت **تُتجاهَل بالكامل منذ بداية
المشروع**، بغض النظر عن Tesseract أو Gemini — الشرط في `documents.py`
كان `if ext == ".pdf":` فقط يُغلِّف كل منطق استخراج النص، فلا يحدث أي شيء
للصور إطلاقًا (لا حتى محاولة فاشلة). أُضيفت دالة `extract_image_page()`
منفصلة تمامًا تعالج هذا المسار، **مُختبَرة فعليًا** برفع JPG حقيقي عبر
الـ API والتحقق من إنشاء مقطع فعلي (كان صفرًا دائمًا قبل الإصلاح).

**تحوّل معماري مهم**: كان الإصدار السابق يعتمد على Tesseract OCR، الذي
اكتُشف فعليًا أنه **لا يعمل على Render** (ومنصات استضافة قياسية مشابهة)
لأنه يحتاج برنامج نظام خارجي (`tesseract-ocr`) لا يمكن تثبيته بسهولة في
بيئة Python العادية هناك — فيفشل صامتًا دائمًا على الإنتاج رغم نجاحه محليًا.

**الحل**: استبدال Tesseract بالكامل بقدرة Gemini الأصلية على قراءة الصور
(Multimodal Vision) — نفس مزوّد الذكاء الاصطناعي المُستخدَم أصلًا للمحادثة
والتلخيص، بدون أي إعداد إضافي:
- **لا يحتاج أي برنامج نظام خارجي** — يعمل مباشرة على Render وأي استضافة
  قياسية أخرى بلا استثناءات
- **جودة أعلى عمليًا من Tesseract**، خصوصًا لخطوط اليد أو جودة مسح متوسطة
- تصيير الصفحة كصورة يتم عبر `PyMuPDF` (مكتبة Python خالصة ذاتية الاكتفاء
  — Wheel مُجمَّع مسبقًا، بلا حاجة لـ `poppler` كما كان الحال سابقًا)

**تم التحقق فعليًا بطريقتين**:
1. اختبار حقيقي (`test_manual.py`) يتحقق أن الرفع ينجح بأمان بلا مفتاح
   Gemini حقيقي (تراجع سليم، صفر مقاطع، بلا انهيار).
2. اختبار بعميل Gemini مُزيَّف (`test_vision_ocr.py`) يتحقق أن الصورة
   تُصيَّر فعليًا بالحجم والترميز الصحيحين (PNG)، وتصل لواجهة Gemini
   بالتنسيق الصحيح (`Part.from_bytes`)، والنص المُستخرَج يُحفَظ كمقطع
   فعلي في قاعدة البيانات — **بدون الحاجة لمفتاح حقيقي** لأن هذا اختبار
   لصحة بناء الاستدعاء نفسه.

```bash
python test_vision_ocr.py
```

قيود معروفة:
- حد أقصى 15 صفحة لكل ملف (`MAX_VISION_PAGES` في `text_extraction.py`)
  — كل صفحة تستهلك استدعاء Gemini واحدًا، فهذا يحمي من استهلاك مفرط لحصة
  Gemini على ملف واحد ضخم بالكامل ممسوح ضوئيًا.
- لم تُختبَر دقة القراءة على نص عربي حقيقي بخط يدوي أو جودة مسح رديئة
  فعليًا بمفتاح حقيقي — فقط منطق بناء الاستدعاء تم التحقق منه.
- يستهلك من نفس حصة Gemini المستخدَمة للمحادثة/التلخيص (وليس مجانيًا
  بشكل منفصل كما كان Tesseract المحلي).

## التشغيل محليًا (الإعداد البسيط — SQLite بلا Redis)

```bash
python3 -m venv venv
source venv/bin/activate        # على Windows: venv\Scripts\Activate.ps1
pip install -r requirements.txt

cp .env.example .env
# عدّل SECRET_KEY، وأضف GEMINI_API_KEY لتفعيل ميزات الذكاء الاصطناعي فعليًا

uvicorn app.main:app --reload
```

هذا الإعداد الأبسط **لا يزال يعمل بلا أي كسر** بعد كل إضافات الإنتاج —
تم التحقق من ذلك صراحةً في نفس هذه الجلسة.

بعدها:
- التوثيق التفاعلي (Swagger): http://127.0.0.1:8000/docs
- فحص الصحة: http://127.0.0.1:8000/

## تشغيل التحقق اليدوي (بدون فتح سيرفر)

```bash
pip install -r requirements-dev.txt   # يضيف reportlab لتوليد PDF تجريبي بنص حقيقي
python test_manual.py
```

ينفّذ 73 سيناريو (Auth، رفع الملفات، **OCR فعلي على PDF بلا طبقة نص**،
مسارات AI، خوض/تصحيح/حفظ اختبار كامل، بطاقات تعليمية، Spaced Repetition،
جلسات دراسة حقيقية مع سقف أمان) ويطبع ✅/❌ لكل واحد.

**لاختباره ضد PostgreSQL وRedis حقيقيَّين** (وليس الإعداد الافتراضي البسيط):
```bash
export TEST_DATABASE_URL="postgresql://user:pass@localhost:5432/your_test_db"
export REDIS_URL="redis://localhost:6379/0"
alembic upgrade head    # أنشئ المخطط أولًا عبر Alembic
python test_manual.py
```

## التحقق من مفتاح Gemini مباشرةً (خارج التطبيق)

```bash
python diagnose_gemini.py
```

يستدعي Gemini مباشرة (`generate_content` و`embed_content`) ويطبع الخطأ
الكامل من Google دون أي تصفية — هذا ما كشف فعليًا أن `gemini-2.0-flash`
توقّف عن الدعم. استخدمه أولًا عند أي مشكلة غامضة (502) قبل تفحّص باقي الكود.

## البنية

```
app/
├── core/
│   ├── config.py       إعدادات من متغيرات بيئة (لا مفاتيح داخل الكود — §30)
│   ├── database.py     اتصال SQLAlchemy + جلسة DB (SQLite أو PostgreSQL)
│   ├── security.py     تشفير كلمات المرور (bcrypt) + JWT (access + refresh)
│   └── rate_limit.py   Rate Limiting عبر Redis مع تراجع تلقائي للذاكرة
├── models/
│   ├── user.py            User (name, email, hashed_password Nullable, google_id)
│   ├── document.py, document_chunk.py
│   ├── quiz.py            Quiz, Question, QuizAttempt (§24)
│   ├── flashcard.py        Flashcard (§24)
│   ├── chat_message.py     ChatMessage — سجل محادثة دائم
│   └── study_session.py    StudySession — وقت دراسة حقيقي (§21)
├── schemas/              نماذج Pydantic لطلبات/ردود الـ API
├── services/
│   ├── pdf_service.py             عدد صفحات PDF
│   ├── text_extraction.py         استخراج نصي مباشر + تراجع تلقائي لـ OCR (§7)
│   ├── chunking.py                تقسيم النص إلى مقاطع متداخلة
│   ├── ai_service.py              الغلاف الوحيد لاستدعاءات Gemini + ذاكرة محادثة (§26, §30)
│   ├── google_auth.py             التحقق من ID Token مع خوادم Google
│   ├── retrieval.py               منطق RAG: بحث دلالي + تراجع لكلمات مفتاحية
│   ├── quiz_generation.py         توليد أسئلة MCQ + تحليل JSON دفاعي
│   └── flashcard_generation.py    توليد بطاقات تعليمية + تحليل JSON دفاعي
├── routers/
│   ├── auth.py             /api/auth/* (register, login, google, refresh, me)
│   ├── documents.py        /api/documents/* (بما فيها /summary)
│   ├── ai.py                /api/ai/chat, /api/ai/chat/history
│   ├── quizzes.py           /api/documents/{id}/questions, /api/quizzes/*
│   ├── flashcards.py        /api/documents/{id}/flashcards (?due_only=), /api/flashcards/*
│   ├── progress.py          /api/progress
│   └── study_sessions.py    /api/study-sessions/start, /{id}/end (§21)
└── main.py                نقطة الدخول + CORS + تطبيق Alembic Migrations تلقائيًا عند البدء

migrations/                    إعداد Migrations (تُطبَّق تلقائيًا عند بدء التشغيل — راجع القسم أعلاه)
diagnose_gemini.py              سكربت تشخيص مباشر لمشاكل Gemini API
test_manual.py                  73 اختبارًا شاملة (Auth, Documents, Quiz...)
test_conversation_memory.py     6 اختبارات لذاكرة المحادثة (بعميل Gemini مُزيَّف)
test_google_auth.py             7 اختبارات لـ Google Sign-In (بتحقق Google مُزيَّف)
```


## ❌ لم يُنفَّذ بعد

- pgvector الفعلي (حاليًا embedding مخزَّن كنص JSON، يعمل بشكل صحيح على
  نطاق صغير لكن بدون تسريع فهرسة Vector حقيقي)
- **دقة OCR على نص عربي حقيقي غير مُختبَرة** (فقط نص إنجليزي مطبوع واضح)
- **مسار Google OAuth الفعلي من الجهاز غير مُختبَر** — فقط منطق الخادم بعد
  وصول رمز صالح (راجع القسم أعلاه للتفاصيل الكاملة)
- Admin Dashboard (§34)، نظام الاشتراكات (§35) — خارج نطاق الأولوية الحالية

## الخطوة التالية المقترحة

1. جرّب `summary`, `questions`, `flashcards` فعليًا من التطبيق (وليس `chat`
   فقط) — آخر جزء من AI غير مُتحقَّق منه بنفس الثقة
2. إذا كنت تخطّط للنشر الفعلي: بدّل `DATABASE_URL` لـ PostgreSQL، شغّل
   `alembic upgrade head`، واضبط `REDIS_URL` — الثلاثة مُختبَرة وجاهزة
3. جرّب رفع PDF ممسوح ضوئيًا حقيقي (وليس نص/صورة تجريبية) من جهازك للتأكد
   من جودة OCR على محتوى واقعي
4. أعدّ Google Cloud Console فعليًا (Web + Android OAuth Client IDs) وجرّب
   "المتابعة عبر Google" من التطبيق الحقيقي — هذا المسار الوحيد المتبقّي
   غير المُختبَر بالكامل في نظام المصادقة
